Changes So Far

We turned the project from a mostly notebook-style setup into a working `src/` training pipeline for the early parts of the plan.

In [preprocessing.py](/C:/Users/meetp/files/PROJECTS/LSTM_jl/src/preprocessing.py), I implemented the Step 3A foundation:
- `chronological_split(...)` splits the dataframe into `train`, `val`, and `test` in time order, with no shuffling.
- `scale_splits_train_only(...)` fits `StandardScaler` only on training data, then transforms validation and test using the train-fitted scalers.
- `assert_no_scaler_leakage(...)` checks that scaler statistics actually match training-only data.
This fixed the original leakage problem from the notebook, where scaling was done on the full dataset before splitting.

In [sequence.py](/C:/Users/meetp/files/PROJECTS/LSTM_jl/src/sequence.py), I added reusable sequence creation:
- `create_sequences(...)` builds `(X, y)` windows from a split.
- `create_sequences_with_past_context(...)` lets validation and test use only prior history for their lookback windows.
This matters because the first validation/test samples still need prior context, but they must not see future rows.

In [train.py](/C:/Users/meetp/files/PROJECTS/LSTM_jl/src/train.py), I first added `PreparedTrainingData` and `prepare_training_data(...)` so the whole no-leakage split/scale/sequence flow can be called from one place. That completed Step 3A in modular form.

Then I implemented Step 3B in the same file:
- `TrainingConfig` from [config.py](/C:/Users/meetp/files/PROJECTS/LSTM_jl/src/config.py) is used to control lookback, split ratios, epochs, batch size, seed, and early stopping patience.
- `set_random_seed(...)` makes runs more reproducible.
- `_require_tensorflow()` gives a clean error if TensorFlow is missing.
- `build_lstm_model(...)` defines the baseline LSTM.
- `train_model(...)` trains on `x_train, y_train` and validates on `x_val, y_val`.
- `train_and_evaluate(...)` evaluates only after training is done.
- `run_training_pipeline(...)` loads a CSV and runs the whole pipeline.
This changed the workflow from “just prepare arrays” to “actually train with validation and reserve test for final evaluation.”

In [evaluate.py](/C:/Users/meetp/files/PROJECTS/LSTM_jl/src/evaluate.py), I implemented Step 3C:
- `inverse_transform_targets(...)` converts scaled values back to real prices.
- `direction_accuracy(...)` measures whether the model gets the up/down movement right.
- `regression_metrics(...)` now returns `mae`, `rmse`, `mape`, and `direction_accuracy`.
- `SplitEvaluation` stores metrics plus original-scale `actuals` and `predictions`.
- `evaluate_split(...)` evaluates model outputs.
- `format_metrics(...)` prints a readable summary.
This made evaluation more realistic and useful, not just training loss.

Then I implemented Step 3D by adding baseline models. In [baselines.py](/C:/Users/meetp/files/PROJECTS/LSTM_jl/src/baselines.py), I added:
- `naive_last_value_baseline(...)`
- `moving_average_baseline(...)`
- `linear_regression_baseline(...)`
In [evaluate.py](/C:/Users/meetp/files/PROJECTS/LSTM_jl/src/evaluate.py), I added `evaluate_predictions(...)` so precomputed baseline predictions can be scored the same way as the LSTM. In [train.py](/C:/Users/meetp/files/PROJECTS/LSTM_jl/src/train.py), I added:
- `baseline_results` into `TrainingArtifacts`
- `run_baselines(...)`
- printing of LSTM vs baseline validation/test metrics in `run_training_pipeline(...)`
This gave us a real benchmark, and the result was important: the current LSTM underperforms the naive and linear baselines on your dataset.

In [requirements.txt](/C:/Users/meetp/files/PROJECTS/LSTM_jl/requirements.txt), I added the dependencies the code now relies on:
- `numpy`
- `pandas`
- `scikit-learn`
- `tensorflow`

In [plan.txt](/C:/Users/meetp/files/PROJECTS/LSTM_jl/plan.txt), we also updated the project plan and agreed to treat it as the canonical plan for this repo.

Step 4 Changes

In [features.py](/C:/Users/meetp/files/PROJECTS/LSTM_jl/src/features.py), I replaced the placeholder with real feature engineering code:
- `add_price_features(...)` now adds `return_1d`, `ma_7`, `ma_20`, `ma_50`, `volatility_7`, `high_low_spread`, `close_open_spread`, and `lag_1` to `lag_3`.
- `finalize_features(...)` applies feature generation and drops the top rows made incomplete by rolling windows and lag columns.
This changed the model input from a single `close` series to a richer multivariate feature set.

In [data_loader.py](/C:/Users/meetp/files/PROJECTS/LSTM_jl/src/data_loader.py), I added `load_stock_csv(...)`:
- reads the CSV
- parses `date` as datetime when present
- sorts the dataset chronologically
- resets the index
This keeps the time-series assumptions intact before splitting and sequence creation.

In [config.py](/C:/Users/meetp/files/PROJECTS/LSTM_jl/src/config.py), I expanded `feature_cols` from `("close",)` to the full engineered feature list:
- `open`
- `high`
- `low`
- `close`
- `volume`
- `return_1d`
- `ma_7`
- `ma_20`
- `ma_50`
- `volatility_7`
- `high_low_spread`
- `close_open_spread`
- `lag_1`
- `lag_2`
- `lag_3`
This tells the scaling, sequence, LSTM, and baseline logic to use the richer feature matrix.

In [train.py](/C:/Users/meetp/files/PROJECTS/LSTM_jl/src/train.py), I made the pipeline feature-aware:
- changed the default `target_col` in `prepare_training_data(...)` to `close`
- imported and used `load_stock_csv(...)` instead of raw `pd.read_csv(...)`
- imported and used `finalize_features(...)` before training begins
- updated `train_and_evaluate(...)` to compute the correct baseline target feature index dynamically from `prepared.feature_cols`
This ensured that the naive and moving-average baselines still read the true target column after the input feature order became multivariate.

After Step 4, I reran the LSTM-vs-baselines comparison on the engineered feature set. The code executed end to end, but the current LSTM still underperformed the simpler baselines. That result was useful, because it confirmed that richer features alone were not enough to make the LSTM competitive yet.

Step 5 Changes

In [config.py](/C:/Users/meetp/files/PROJECTS/LSTM_jl/src/config.py), I expanded `TrainingConfig` so model and training settings are configurable rather than hardcoded:
- increased default `epochs` from `20` to `50`
- increased `early_stopping_patience` from `5` to `8`
- added `learning_rate`
- added `lstm_units_1`
- added `lstm_units_2`
- added `dense_units`
- added `dropout_rate`
- added `models_dir`
- added `results_dir`
- added `checkpoint_name`
This made the training process tunable and reusable without editing the model code every time.

In [train.py](/C:/Users/meetp/files/PROJECTS/LSTM_jl/src/train.py), I upgraded the LSTM training flow:
- `build_lstm_model(...)` now accepts `config`
- the model now uses configurable LSTM layer sizes
- I added dropout after each LSTM layer
- I switched the optimizer setup to configurable Adam learning rate
- `train_model(...)` now creates the models directory if needed
- I added `ModelCheckpoint(...)` alongside `EarlyStopping(...)`
- the best validation model is now written to `models/best_lstm.keras`
This implemented the core Step 5 goals: stronger training controls, configurable hyperparameters, and checkpointing based on validation loss.

Step 6 Changes

In [utils.py](/C:/Users/meetp/files/PROJECTS/LSTM_jl/src/utils.py), I replaced the empty file with artifact-saving helpers:
- `ensure_dir(...)`
- `save_pickle(...)`
- `save_json(...)`
These helpers are used by the training pipeline to persist model outputs and metadata in a consistent format.

In [train.py](/C:/Users/meetp/files/PROJECTS/LSTM_jl/src/train.py), I added `save_training_artifacts(...)`:
- saves the trained model to `models/lstm_model.keras`
- saves the feature scaler to `models/feature_scaler.pkl`
- saves the target scaler to `models/target_scaler.pkl`
- saves metadata to `models/metadata.json`
- saves metrics and training history to `results/metrics.json`
- saves actuals and predictions to `results/predictions.json`
I also updated `run_training_pipeline(...)` to accept `save_artifacts=True` and print the saved paths after training.

During verification of Step 5 and Step 6:
- the code compiled successfully with `python -m py_compile`
- `ModelCheckpoint` successfully created `models/best_lstm.keras`
- the full default feature-rich training run exceeded the environment timeout, so I could not complete a full default run inside this session
- I separately verified the artifact-writing path and confirmed the expected files were created under `models/` and `results/`

The saved files confirmed after Step 6 were:
- [best_lstm.keras](/C:/Users/meetp/files/PROJECTS/LSTM_jl/models/best_lstm.keras)
- [lstm_model.keras](/C:/Users/meetp/files/PROJECTS/LSTM_jl/models/lstm_model.keras)
- [feature_scaler.pkl](/C:/Users/meetp/files/PROJECTS/LSTM_jl/models/feature_scaler.pkl)
- [target_scaler.pkl](/C:/Users/meetp/files/PROJECTS/LSTM_jl/models/target_scaler.pkl)
- [metadata.json](/C:/Users/meetp/files/PROJECTS/LSTM_jl/models/metadata.json)
- [metrics.json](/C:/Users/meetp/files/PROJECTS/LSTM_jl/results/metrics.json)
- [predictions.json](/C:/Users/meetp/files/PROJECTS/LSTM_jl/results/predictions.json)

One important practical note from Steps 5 and 6:
- the implementation is in place and verified at the file/output level
- the current default training configuration is now heavy enough that it hits the environment timeout
- that means the next pragmatic improvement is likely to add a lightweight debug config for faster verification runs while preserving the richer production-style defaults

Step 8 Changes

In [app/backend/schemas.py](/C:/Users/meetp/files/PROJECTS/LSTM_jl/app/backend/schemas.py), I expanded the backend API contracts:
- added `ErrorResponse`
- enriched `ModelInfoResponse` with `available_baselines`, `artifacts_ready`, and `prediction_mode`
- added `ForecastRequest`
- added `ForecastPoint`
- added `ForecastResponse`
This made the API more explicit and better aligned with the backend plan, especially for multi-step forecasting and frontend status handling.

In [app/backend/services.py](/C:/Users/meetp/files/PROJECTS/LSTM_jl/app/backend/services.py), I restructured the backend into a more service-oriented API layer:
- added `AVAILABLE_BASELINES`
- added cached artifact loading with `load_prediction_artifacts()` using `lru_cache`
- added `artifacts_ready()`
- split prediction preparation into `_prepare_recent_window(...)`, `_compute_baseline_predictions(...)`, and `_predict_with_model(...)`
- kept `predict_next_day(...)` but refactored it to use the new helpers
- added `forecast_prices(...)` for backend multi-step forecasts
- expanded `get_model_info()` so the app can tell whether the backend is serving a trained LSTM or a fallback mode
These changes reduced repeated model/scaler loading, improved service boundaries, and prepared the backend for richer frontend usage.

In [app/backend/main.py](/C:/Users/meetp/files/PROJECTS/LSTM_jl/app/backend/main.py), I added the `POST /forecast` endpoint and wired it to `forecast_prices(...)`. I also registered the forecast schemas and the 400 error model there. The backend route set now includes:
- `/health`
- `/model-info`
- `/history`
- `/predict`
- `/forecast`
- `/metrics`
- `/comparison`

In [tests/test_backend_api.py](/C:/Users/meetp/files/PROJECTS/LSTM_jl/tests/test_backend_api.py), I added a backend API test suite covering:
- `/health`
- `/model-info`
- `/history`
- `/predict`
- `/forecast`

In [requirements.txt](/C:/Users/meetp/files/PROJECTS/LSTM_jl/requirements.txt), I added `pytest` for backend testing.

During Step 8 verification:
- the backend code compiled cleanly
- service-level smoke tests confirmed `/model-info` and forecast logic worked
- once I switched to the `.venv` interpreter explicitly, the backend API tests passed:
  - `5 passed` for `tests/test_backend_api.py`
- the only warning was a pytest cache permission warning that did not affect test results

Step 9 Changes

In [app/frontend/streamli_app.py](/C:/Users/meetp/files/PROJECTS/LSTM_jl/app/frontend/streamli_app.py), I rebuilt the Streamlit UI into the dashboard structure described in the plan:
- added stronger sidebar controls for ticker, lookback days, forecast horizon, and evaluation split
- added a model status panel showing `prediction_mode`, `artifacts_ready`, available baselines, and the disclaimer
- added a historical price chart section
- added a volume chart section
- added a next-day prediction summary with delta from latest close
- added baseline prediction tables
- added a forecast section powered by `/forecast`
- added an actual-vs-predicted section
- replaced raw JSON metrics with metric cards for MAE, RMSE, MAPE, and direction accuracy
- added a baseline metrics table
- added explainability and limitations sections
This changed the frontend from a thin API viewer into a real dashboard.

I also added frontend helper functions inside [app/frontend/streamli_app.py](/C:/Users/meetp/files/PROJECTS/LSTM_jl/app/frontend/streamli_app.py):
- `fetch_json(...)`
- `metric_card_columns(...)`
- `build_baseline_prediction_table(...)`
- `build_baseline_metric_table(...)`
- `build_forecast_frame(...)`
These keep the UI logic organized and easier to extend.

One important structural fix in Step 9 was wrapping the page in `main()` and only calling it under `if __name__ == "__main__":`. That made the module import-safe for checks and tooling instead of making backend requests at import time.

During Step 9 verification:
- the frontend compiled cleanly with the `.venv` interpreter
- the module imported successfully after the `main()` refactor

App Run/Stop and Launch Verification Changes

After Step 9, I added convenience scripts so the app can be started and stopped more easily:

In [run_app.ps1](/C:/Users/meetp/files/PROJECTS/LSTM_jl/run_app.ps1), I added a launcher that:
- uses `.\.venv\Scripts\python.exe` explicitly
- starts FastAPI on `127.0.0.1:8000`
- starts Streamlit on `127.0.0.1:8501`
- prints the URLs to open

In [stop_app.ps1](/C:/Users/meetp/files/PROJECTS/LSTM_jl/stop_app.ps1), I added a matching shutdown script that:
- finds `.venv` Python processes for `uvicorn app.backend.main:app`
- finds `.venv` Python processes for `streamlit run app/frontend/streamli_app.py`
- stops them by PID

I also verified the combined app stack over live HTTP:
- FastAPI `/health` returned `200`
- Streamlit root page returned `200`
- `/model-info` returned `prediction_mode`
- `/predict` returned a valid prediction payload
- `/forecast` returned the expected number of points
At the time of that verification, the app was still using `naive_last_value_fallback` because the saved trained artifacts were not aligned with the latest feature-engineered training pipeline.

Step 10 Changes

In [src/config.py](/C:/Users/meetp/files/PROJECTS/LSTM_jl/src/config.py), I added ingestion-related settings:
- `data_dir`
- `raw_data_path`
- `refresh_metadata_path`
- `default_ticker`
- `live_data_enabled`
This moved dataset assumptions out of the backend and training code.

In [src/data_loader.py](/C:/Users/meetp/files/PROJECTS/LSTM_jl/src/data_loader.py), I expanded the file into a real ingestion module:
- added `validate_stock_dataframe(...)`
- added `load_local_dataset(...)`
- added `get_available_tickers_from_data(...)`
- added `save_stock_csv(...)`
- added `build_refresh_metadata(...)`
- added `save_refresh_metadata(...)`
- added `load_refresh_metadata(...)`
The loader now validates required columns, empty data, duplicate date rows, missing dates, and missing numeric values before the rest of the pipeline uses the dataset.

In [src/update_data.py](/C:/Users/meetp/files/PROJECTS/LSTM_jl/src/update_data.py), I added a refresh utility that:
- loads and validates the canonical local dataset
- builds refresh metadata
- writes `data/refresh_metadata.json`
I also fixed it so it can be run directly from the repo root with the `.venv` interpreter.

In [src/train.py](/C:/Users/meetp/files/PROJECTS/LSTM_jl/src/train.py), I updated `run_training_pipeline(...)` so it uses `load_local_dataset(...)` instead of directly reading a CSV with `pd.read_csv(...)`. That means training now goes through the same ingestion/validation path as the app.

In [app/backend/services.py](/C:/Users/meetp/files/PROJECTS/LSTM_jl/app/backend/services.py), I refactored the backend to depend on the ingestion layer instead of a single hardcoded file path:
- removed the old direct `DATA_PATH` usage
- dataset loading now uses `load_local_dataset(...)`
- ticker discovery now uses `get_available_tickers_from_data(...)`
- model info now includes refresh metadata loaded from `data/refresh_metadata.json`

In [app/backend/schemas.py](/C:/Users/meetp/files/PROJECTS/LSTM_jl/app/backend/schemas.py), I updated `ModelInfoResponse` so it can expose `data_refresh`.

During Step 10 verification:
- the updated ingestion and backend files compiled successfully
- `src/update_data.py` ran successfully with the `.venv` interpreter
- [refresh_metadata.json](/C:/Users/meetp/files/PROJECTS/LSTM_jl/data/refresh_metadata.json) was created with:
  - `source_type: csv`
  - `row_count: 1259`
  - `tickers: ['MSFT']`
  - `min_date: 2013-02-08`
  - `max_date: 2018-02-07`
  - `last_refresh_utc: ...`
- backend ingestion smoke tests confirmed ticker discovery and model info still worked after the refactor

Step 11 Changes

In [tests/conftest.py](/C:/Users/meetp/files/PROJECTS/LSTM_jl/tests/conftest.py), I added shared pytest fixtures:
- `sample_stock_df`
- `scaled_target_scaler`
These fixtures reduce duplication across the test suite and make the tests easier to read and maintain.

I added unit tests for the preprocessing layer in [test_preprocessing.py](/C:/Users/meetp/files/PROJECTS/LSTM_jl/tests/test_preprocessing.py):
- verified chronological split sizes
- verified train/val/test order is preserved
- verified train-only scaling statistics
These protect the no-leakage foundation from Step 3A.

I added sequence tests in [test_sequence.py](/C:/Users/meetp/files/PROJECTS/LSTM_jl/tests/test_sequence.py):
- verified standard sequence output shapes
- verified mismatched input lengths raise errors
- verified `create_sequences_with_past_context(...)` uses prior history correctly
These protect the lookback/window logic used by training and evaluation.

I added feature engineering tests in [test_features.py](/C:/Users/meetp/files/PROJECTS/LSTM_jl/tests/test_features.py):
- verified all engineered columns are created
- verified `finalize_features(...)` drops NaN rows from rolling/lags
These protect the Step 4 feature pipeline.

I added ingestion tests in [test_data_loader.py](/C:/Users/meetp/files/PROJECTS/LSTM_jl/tests/test_data_loader.py):
- validated good stock data
- rejected missing columns
- verified ticker extraction
- verified refresh metadata save/load behavior
This protects the Step 10 ingestion and validation layer.

I added evaluation tests in [test_evaluate.py](/C:/Users/meetp/files/PROJECTS/LSTM_jl/tests/test_evaluate.py):
- verified expected regression metric keys
- verified direction accuracy behavior
- verified `evaluate_predictions(...)` returns the expected `SplitEvaluation` structure
These protect the Step 3C and Step 3D evaluation logic.

I added baseline tests in [test_baselines.py](/C:/Users/meetp/files/PROJECTS/LSTM_jl/tests/test_baselines.py):
- verified naive baseline output
- verified moving average baseline output
- verified linear regression baseline prediction shape
These protect the LSTM-vs-baseline comparison logic from Step 3D.

I added training helper tests in [test_train.py](/C:/Users/meetp/files/PROJECTS/LSTM_jl/tests/test_train.py):
- verified `prepare_training_data(...)` returns 3D sequence tensors
- verified `run_baselines(...)` returns all expected baseline names
These protect the orchestration layer without requiring long TensorFlow training runs.

I expanded the backend API tests in [test_backend_api.py](/C:/Users/meetp/files/PROJECTS/LSTM_jl/tests/test_backend_api.py):
- kept coverage for `/health`, `/model-info`, `/history`, `/predict`, and `/forecast`
- added `/metrics`
- added `/comparison`
- added invalid ticker handling for `/predict`
- verified `data_refresh` is present in `/model-info`
This protects the frontend/backend contract introduced in Steps 8, 9, and 10.

During Step 11 verification:
- the test files compiled successfully
- I ran the full suite with the project `.venv`:
  - `.\.venv\Scripts\python.exe -m pytest tests -q`
- final result:
  - `26 passed`
- one pytest cache warning remained because this environment cannot write `.pytest_cache` cleanly, but it did not affect test execution

Step 11 completed with a passing multi-layer test suite covering the ML core, ingestion layer, and backend API.

Step 12 Changes

In [src/logging_config.py](/C:/Users/meetp/files/PROJECTS/LSTM_jl/src/logging_config.py), I added centralized logging helpers:
- `configure_logging()`
- `get_logger()`
This gave the project one shared logging format instead of scattered print/debug output.

In [app/backend/main.py](/C:/Users/meetp/files/PROJECTS/LSTM_jl/app/backend/main.py), I added request timing middleware and endpoint-level logging:
- every request now logs method, path, status code, and duration in milliseconds
- `/health`, `/model-info`, `/history`, `/predict`, `/forecast`, `/metrics`, and `/comparison` now log success/failure events
This implemented the request-time and latency visibility described in the plan.

In [app/backend/services.py](/C:/Users/meetp/files/PROJECTS/LSTM_jl/app/backend/services.py), I added service-layer validation and operational logging:
- logs when the base dataset is loaded
- logs missing ticker failures
- logs missing or failed model/scaler artifact loads
- logs when prediction falls back to the naive baseline
- logs when forecast falls back to the naive baseline
- added explicit NaN checking for recent feature windows before scaling/inference
- logs metrics loading
- added loading of `models/metadata.json` into the `/model-info` response as `model_metadata`
This made backend failures and fallback behavior explicit instead of silent.

Because `/model-info` now includes model metadata, I updated [app/backend/schemas.py](/C:/Users/meetp/files/PROJECTS/LSTM_jl/app/backend/schemas.py) so `ModelInfoResponse` includes:
- `model_metadata`

In [src/data_loader.py](/C:/Users/meetp/files/PROJECTS/LSTM_jl/src/data_loader.py), I added ingestion logging:
- logs CSV load start
- logs successful dataframe validation
- logs saved CSV writes
- logs refresh metadata writes
This makes ingestion and data validation behavior observable.

In [src/update_data.py](/C:/Users/meetp/files/PROJECTS/LSTM_jl/src/update_data.py), I added refresh lifecycle logs:
- refresh start with dataset path
- refresh completion with row count, min date, max date, and metadata path
This makes metadata refresh operations traceable.

In [src/train.py](/C:/Users/meetp/files/PROJECTS/LSTM_jl/src/train.py), I added training lifecycle logging:
- logs training start with row count, lookback, feature count, epochs, and batch size
- logs prepared train/val/test sizes
- logs training completion and checkpoint path
- logs artifact save completion
- logs training pipeline completion with validation and test metrics
This makes the training flow much easier to debug and monitor.

During Step 12 verification:
- all updated files compiled successfully
- `src/update_data.py` ran cleanly and emitted the new ingestion logs
- backend service smoke tests worked and now log meaningful fallback/error details
- the new logs surfaced a useful issue clearly:
  - the current `models/best_lstm.keras` artifact is not loadable
  - backend prediction therefore falls back intentionally to `naive_last_value_fallback`
- I reran the full test suite:
  - `.\.venv\Scripts\python.exe -m pytest tests -q`
  - result: `26 passed`

Step 12 completed with centralized logging, request timing, clearer validation/fallback handling, and a still-passing test suite.

Step 13 Changes

In [README.md](/C:/Users/meetp/files/PROJECTS/LSTM_jl/README.md), I replaced the empty file with a full project guide that now documents:
- project overview
- current implementation status
- folder structure
- local setup using `.venv`
- how to run the app
- how to run the training and data refresh utilities
- evaluation methodology
- backend API summary
- frontend dashboard summary
- testing
- logging
- limitations
- disclaimer
- screenshot placeholders
- links to `plan.txt` and `executed.txt`
This made the repo understandable without needing chat history.

In [CHANGELOG.md](/C:/Users/meetp/files/PROJECTS/LSTM_jl/CHANGELOG.md), I added a concise project changelog summarizing the major milestone groups:
- Step 3A-3D
- Step 4-6
- Step 8-10
- Step 11-12
This gives the repo a cleaner high-level history of what was implemented.

In [CONTRIBUTING.md](/C:/Users/meetp/files/PROJECTS/LSTM_jl/CONTRIBUTING.md), I added a contributor workflow guide covering:
- `.venv` usage
- running the app
- stopping the app
- running tests
- refreshing data
- general development expectations
This made the repo feel more complete and easier to work in.

During Step 13 verification:
- I read back all three documentation files
- I cross-checked them against the current repo contents
- the documented commands and files matched the current project structure

Step 13 completed as a documentation pass across the repo.

Step 14 Changes

Initial backend Docker work:

In [Dockerfile.backend](/C:/Users/meetp/files/PROJECTS/LSTM_jl/Dockerfile.backend), the first backend image draft was added. It already had the correct base image and uvicorn startup command, but it still needed improvements.

Backend Docker improvements:

I updated [Dockerfile.backend](/C:/Users/meetp/files/PROJECTS/LSTM_jl/Dockerfile.backend) to make it more professional:
- switched it to install from `requirements-backend.txt`
- added creation of a non-root user `appuser`
- switched the container to run as `appuser`
- added a backend `HEALTHCHECK` against `http://127.0.0.1:8000/health`
- kept uvicorn binding on `0.0.0.0:8000`
These changes made the backend image smaller, safer, and more deployment-friendly.

In [.dockerignore](/C:/Users/meetp/files/PROJECTS/LSTM_jl/.dockerignore), I tightened the Docker build context:
- excluded Python/cache/log files
- excluded `.venv`
- excluded notebooks and notebook checkpoints
- excluded `test_artifacts_temp/`
- updated the Docker ignore entry to match `compose.yaml`
This reduces image bloat and prevents local junk from being copied into Docker builds.

Dependency split for deployment:

I reviewed and aligned the split dependency files:
- [requirements-backend.txt](/C:/Users/meetp/files/PROJECTS/LSTM_jl/requirements-backend.txt) is now for backend runtime dependencies
- [requirements-frontend.txt](/C:/Users/meetp/files/PROJECTS/LSTM_jl/requirements-frontend.txt) is now for frontend runtime dependencies
- [requirements-dev.txt](/C:/Users/meetp/files/PROJECTS/LSTM_jl/requirements-dev.txt) was added for development/test dependencies

I specifically removed `pytest` from [requirements-frontend.txt](/C:/Users/meetp/files/PROJECTS/LSTM_jl/requirements-frontend.txt) and moved it into [requirements-dev.txt](/C:/Users/meetp/files/PROJECTS/LSTM_jl/requirements-dev.txt).

Frontend deployment work:

I added [Dockerfile.frontend](/C:/Users/meetp/files/PROJECTS/LSTM_jl/Dockerfile.frontend):
- installs from `requirements-frontend.txt`
- sets `API_BASE_URL=http://backend:8000`
- creates and uses non-root user `appuser`
- exposes port `8501`
- adds a Streamlit healthcheck on `/_stcore/health`
- starts Streamlit with `--server.address=0.0.0.0`
This made the frontend independently deployable as its own service.

I updated [app/frontend/streamli_app.py](/C:/Users/meetp/files/PROJECTS/LSTM_jl/app/frontend/streamli_app.py) so the frontend backend URL is now environment-driven:
- replaced the hardcoded `http://127.0.0.1:8000`
- now uses `API_BASE_URL = os.getenv("API_BASE_URL", "http://127.0.0.1:8000")`
This is required for separate frontend/backend deployment.

Compose orchestration:

I added [compose.yaml](/C:/Users/meetp/files/PROJECTS/LSTM_jl/compose.yaml) with two services:
- `backend`
- `frontend`

The compose file:
- builds each service from its own Dockerfile
- maps ports `8000:8000` and `8501:8501`
- passes `API_BASE_URL=http://backend:8000` into the frontend
- waits for backend health before starting the frontend with `depends_on`
- adds healthchecks to both services
This gives the repo a local multi-service deployment workflow that mirrors the intended production split.

Documentation updates for deployment:

I updated [README.md](/C:/Users/meetp/files/PROJECTS/LSTM_jl/README.md) again so the Docker deployment path is documented:
- split dependency installation instructions
- Docker deployment section
- descriptions of `Dockerfile.backend`, `Dockerfile.frontend`, `compose.yaml`, and `.dockerignore`
- `docker compose up --build`
- `docker compose down`
- notes about `API_BASE_URL`
- explanation of the split requirements files

Verification and review findings during Step 14:

- I confirmed Docker was installed locally, but direct Docker build verification from the sandbox was limited by local Docker permission issues under `C:\Users\meetp\.docker\...`
- I reviewed the backend image and identified the following improvements, which were then implemented:
  - missing `.dockerignore`
  - backend image installing frontend/test dependencies
  - missing backend healthcheck
  - running as root
- later, when you ran `docker compose up --build`, Docker reported that `pytest-cache-files-*` directories were still being pulled into the build context; that confirmed why `.dockerignore` completeness matters in this repo

Step 14 completed on the repo side with:
- separate backend/frontend Dockerfiles
- split deployment requirements
- Docker build context cleanup
- compose-based two-service orchestration
- env-based frontend/backend wiring
- deployment documentation in README
